import styles from '../../styles/Home.module.css'

export default function Feed() {
    return(
        <div>
        
        <div className={styles.walletButton}>
        <h1 className={styles.logo}>colorpixels</h1>
            <button >Connect Wallet</button>
        </div>
        </div>
    )
}